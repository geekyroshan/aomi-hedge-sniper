{
  "name": "hedge-sniper",
  "version": "0.1.0",
  "private": true,
  "description": "AI-found, positive-EV Polymarket hedges via the AOMI on-chain agent runtime.",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "start": "pnpm --filter web start",
    "typecheck": "pnpm --filter web typecheck",
    "verify-env": "tsx scripts/verify-env.ts",
    "seed": "tsx scripts/seed-hedge-demo.ts"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@10.30.3",
  "engines": {
    "node": ">=22"
  }
}
